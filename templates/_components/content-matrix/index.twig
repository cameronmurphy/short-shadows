{% for block in props.matrix.all %}
  {% if block.status == 'enabled' %}
    {% switch block.type %}
      {% case 'imageBlock' %}
        {% include '_components/image-block' with { props: block } only %}
    {% endswitch %}
  {% endif %}
{% endfor %}
