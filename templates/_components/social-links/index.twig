{% if socialLinks.facebookLink %}
  {% include '_components/social-links/social-link.twig' with { props: { type: 'facebook', href: socialLinks.facebookLink } } %}
{% endif %}

{% if socialLinks.instagramLink %}
  {% include '_components/social-links/social-link.twig' with { props: { type: 'instagram', href: socialLinks.instagramLink } } %}
{% endif %}
